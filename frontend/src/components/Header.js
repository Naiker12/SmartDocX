import React from 'react';

const Header = () => (
  <header style={{background: '#2d3748', color: '#fff', padding: '1rem', textAlign: 'center', fontSize: '1.5rem', fontWeight: 'bold'}}>
    SmartDocX
  </header>
);

export default Header;
